# Reference outputs

These files were produced by `python -m experiments.run_all` on a single processor
core and are included so that a new run can be compared against them. Rerunning the
suite regenerates every file in this directory.
